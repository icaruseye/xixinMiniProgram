<wxs module="filter" src="../../utils/filter.wxs"></wxs>
<!-- 课程详情 -->
<view class="course_info">
  <view class="content">{{ Desctiption }}</view>
  <view class="imgs" wx:for="{{Img}}" wx:key="{{index}}">
    <image src="{{filter.transformImgUrl(item)}}" mode='widthFix'></image>
  </view>
</view>